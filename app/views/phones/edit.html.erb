<% if signed_in? && current_user.admin? %>
<% provide(:title, 'Edit phone') %>

<div class="page-region">
 <div class="page-region-content">

  <div class="span6 offset3">

<h1>Editing phone</h1>

<%= render 'form' %>

<%= link_to raw("<i class=\"icon-arrow-left-3\"></i>"), phones_path %>
<%= link_to 'Show', @phone %> |
</div>
</div>
</div>
<% else %>
<h1>You are not an admin, and don't try to play smart and enter url by hand</h1>
<% end %>